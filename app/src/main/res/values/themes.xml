<resources>
    <!-- Base application theme. -->
    <style name="Theme.OrderApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red_mix</item>
        <item name="colorPrimaryVariant">#c85a54</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
    </style>

    <!-- Splash Screen theme. -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <!-- Corner radius image. -->
    <style name="CornerRadiusImage">
        <item name="cornerSize">10%</item>
    </style>

    <style name="CornerRadiusImageTop">
        <item name="cornerSizeTopLeft">10%</item>
        <item name="cornerSizeTopRight">10%</item>
    </style>

    <style name="CornerRadiusImageLeft">
        <item name="cornerSizeTopLeft">10%</item>
        <item name="cornerSizeBottomLeft">10%</item>
    </style>

    <style name="MarginTopFiveDP">
        <item name="android:layout_marginTop">5dp</item>
    </style>
</resources>